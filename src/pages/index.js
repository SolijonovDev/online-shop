import { HomeView } from '@/views/HomeView';
import { Fragment } from 'react';

const HomePage = () => {
  return (
    <Fragment>
      <HomeView />
    </Fragment>
  );
};

export default HomePage;
